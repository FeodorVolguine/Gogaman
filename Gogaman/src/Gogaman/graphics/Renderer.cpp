#include "pch.h"
#include "Renderer.h"

#include "RenderCommand.h"

namespace Gogaman
{
	void Renderer::AddVertexArrayBuffer(VertexArrayBuffer &&vertexArrayBuffer)
	{
		//RenderCommand::RenderIndexed(std::move(vertexArrayBuffer));
		//m_RenderCommands.emplace(std::move(command));
	}
}